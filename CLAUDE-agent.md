# adam-abundance — Agent Instructions

## Identity
- **Agent:** adam-abundance
- **Owner:** Andrew Marks (andrew.marks@valnetinc.com)
- **Created:** 2026-07-13
- **Template:** nextjs-dashboard

## Purpose
Centralized Dashboard to manage all of Auto

## Context
- **Department:** Auto
- **Expected users:** Auto

## Required platforms
Database (Supabase), GitHub, vm

## Folder structure (Drive)
- `Config/`             — PRD and reference documents
- `Config/Input Files/` — uploaded data files
- `Projects/`           — code (mirrors the GitHub repo)
- `Outputs/`            — reports, exports, results
- `docs/`               — architecture notes

## Rules
- Read the workspace `CLAUDE.md` first.
- Credentials live in `~/.claude/.env` on the local machine only — never in Drive or the repo.
- Save all outputs to the `Outputs/` folder.
- Never commit credentials, tokens, or secrets to the repo.

## Workflow
- All changes go through Claude Code in your local agent folder, then `git push` to the `claude-space` org repo.
- The deploy webhook on the VM auto-pulls + restarts on push to `main`.
- Drive ↔ GitHub mirror runs every 10 minutes; push manually via the Push-to-GitHub button if you need a faster cycle.

*Generated by ShellAgent on 2026-07-13*
